name: whos-hot-nhl

services:
  postgres:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_DB: nhl_stats
      POSTGRES_USER: whoshot
      POSTGRES_PASSWORD: ${DB_PASSWORD:-whoshot_local}
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U whoshot -d nhl_stats"]
      interval: 5s
      timeout: 5s
      retries: 12

volumes:
  postgres_data:
